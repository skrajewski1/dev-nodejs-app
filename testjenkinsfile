
pipeline {
    agent any

    stages {
        stage('Dep install') {
            agent { docker { image 'nodejs'
                args '-u root:root' }}
            steps {
                sh 'npm install'
            }
        }
    }
}
