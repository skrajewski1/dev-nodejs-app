
pipeline {
    agent any

    stages {
        stage('Dep install') {
            agent { docker { image 'nodejs'
                args '-u root:sudo' }}
            steps {
                sh 'npm install'
            }
        }
    }
}
