
pipeline {
    agent { docker { image 'nodejs'
            args '-u root:root' }}

    stages {
        stage('Dep install') {
            steps {
                sh 'npm install'
            }
        }
        stage('package'){
            steps{
                sh 'npm pack'
            }
        }
    }
}
